export const cookieValues = {
  accessToken: "ACCESS_TOKEN",
  refreshToken: "REFRESH_TOKEN",
};

export const urlPrefix = "https://app.nexys.io/api/product/user-management/";

// 15min
export const tokenValidityDefault = 15 * 60;
